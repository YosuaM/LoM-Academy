---
import { advanceMounts } from "./Mounts.astro";
import { artifactMorphs } from "./Artifacts.astro";
import { backAccessoryMorphs } from "./BackAccessories.astro";

export const redSoulTypeTwo = {
    name: "Red Soul Selection 2",
    image: "",
    icon: "/assets/img/game/items/red_soul_selection_icon.webp",
    skill: "Select a Soul from Pal Crit DMG, Skill DMG, Pal DMG, Heealing, Boss DMG, HP Regen, Skill Crit DMG, and Boss DMG RES Souls.",
    effect: "-",
    howToGet: "",
};

export const luckySpinnerItems = [
    // Week 1
    [
        artifactMorphs.find((artifact) => artifact.name === "Chaotic Warlord's Hammer")
    ],
    // Week 2
    [
        advanceMounts.find((mount) => mount.name === "White Tiger")
    ],
    // Week 3
    [
        redSoulTypeTwo
    ],
    // Week 4
    [
        advanceMounts.find((mount) => mount.name === "Blue Ox")
    ],
    // Week 5
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Moonlit Wisp")
    ],
    // Week 6
    [
        advanceMounts.find((mount) => mount.name === "Blue Queen")
    ],
    // Week 7
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Emerald Embrace")
    ],
    // Week 8
    [
        advanceMounts.find((mount) => mount.name === "Round Pepe")
    ],
    // Week 9
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Blade Pursuit")
    ],
    // Week 10
    [
        advanceMounts.find((mount) => mount.name === "Purple Wing")
    ],
    // Week 11
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Moonlit Wisp")
    ],
    // Week 12
    [
        advanceMounts.find((mount) => mount.name === "Magic Carpet")
    ],
    // Week 13
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Emerald Embrace")
    ],
    // Week 14
    [
        advanceMounts.find((mount) => mount.name === "Koi Paper Kite")
    ],
    // Week 15
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Blade Pursuit")
    ],
    // Week 16
    [
        advanceMounts.find((mount) => mount.name === "Mini Motorcycle")
    ],
    // Week 17
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Moonlit Wisp")
    ],
    // Week 18
    [
        artifactMorphs.find((artifact) => artifact.name === "Chaotic Warlord's Hammer"),
        advanceMounts.find((mount) => mount.name === "White Tiger"),
        advanceMounts.find((mount) => mount.name === "Blue Ox")
    ],
    // Week 19
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Emerald Embrace")
    ],
    // Week 20
    [
        advanceMounts.find((mount) => mount.name === "Blue Queen"),
        advanceMounts.find((mount) => mount.name === "Round Pepe"),
        advanceMounts.find((mount) => mount.name === "Purple Wing")
    ],
    // Week 21
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Blade Pursuit")
    ],
    // Week 22
    [
        advanceMounts.find((mount) => mount.name === "Magic Carpet"),
        advanceMounts.find((mount) => mount.name === "Koi Paper Kite"),
        advanceMounts.find((mount) => mount.name === "Mini Motorcycle")
    ],
    // Week 23
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Moonlit Wisp")
    ],
    // Week 24
    [
        artifactMorphs.find((artifact) => artifact.name === "Chaotic Warlord's Hammer"),
        advanceMounts.find((mount) => mount.name === "White Tiger"),
        advanceMounts.find((mount) => mount.name === "Blue Ox")
    ],
    // Week 25
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Emerald Embrace")
    ],
    // Week 26
    [
        advanceMounts.find((mount) => mount.name === "Blue Queen"),
        advanceMounts.find((mount) => mount.name === "Round Pepe"),
        advanceMounts.find((mount) => mount.name === "Purple Wing")
    ],
    // Week 27
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Blade Pursuit")
    ],
    // Week 28
    [
        advanceMounts.find((mount) => mount.name === "Magic Carpet"),
        advanceMounts.find((mount) => mount.name === "Koi Paper Kite"),
        advanceMounts.find((mount) => mount.name === "Mini Motorcycle")
    ],
    // Week 29
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Moonlit Wisp")
    ],
    // Week 30
    [
        artifactMorphs.find((artifact) => artifact.name === "Chaotic Warlord's Hammer"),
        advanceMounts.find((mount) => mount.name === "White Tiger"),
        advanceMounts.find((mount) => mount.name === "Blue Ox")
    ],
    // Week 31
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Emerald Embrace")
    ],
    // Week 32
    [
        advanceMounts.find((mount) => mount.name === "Blue Queen"),
        advanceMounts.find((mount) => mount.name === "Round Pepe"),
        advanceMounts.find((mount) => mount.name === "Purple Wing")
    ],
    // Week 33
    [
        redSoulTypeTwo,
        backAccessoryMorphs.find((backAccessory) => backAccessory.name === "Blade Pursuit")
    ],
    // Week 34
    [
        advanceMounts.find((mount) => mount.name === "Magic Carpet"),
        advanceMounts.find((mount) => mount.name === "Koi Paper Kite"),
        advanceMounts.find((mount) => mount.name === "Mini Motorcycle")
    ],
];
---